:root{
  --bg:#0f1a14;
  --card:#14251c;
  --card2:#173021;
  --text:#eaf7ef;
  --muted:#b8d6c6;
  --green:#1a8f5a;
  --green2:#2dd28a;
  --red:#ff4d4d;
  --gold:#ffd36a;
  --shadow: 0 12px 30px rgba(0,0,0,.35);
  --r:18px;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background:
    radial-gradient(1200px 700px at 20% -10%, rgba(45,210,138,.25), transparent 55%),
    radial-gradient(900px 600px at 110% 0%, rgba(255,211,106,.18), transparent 55%),
    var(--bg);
  color:var(--text);
}

a{color:inherit}
button{font:inherit}

.wrap{max-width:1100px;margin:0 auto;padding:18px}
.topbar{
  display:flex;align-items:center;justify-content:space-between;
  gap:12px; padding:14px 16px; border-radius:22px;
  background:linear-gradient(180deg, rgba(26,143,90,.22), rgba(20,37,28,.55));
  box-shadow: var(--shadow);
  position:sticky; top:10px; z-index:5;
  backdrop-filter: blur(10px);
}
.brand{display:flex;align-items:center;gap:12px}
.brand img{width:44px;height:44px;border-radius:14px;object-fit:cover;background:#0b1510}
.brand .t1{font-weight:900;letter-spacing:.3px}
.brand .t2{font-size:12px;color:var(--muted);margin-top:2px}

.pills{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}
.pill{
  padding:9px 12px;border-radius:999px;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.10);
  color:var(--muted);
}
.pill b{color:var(--text)}

.grid{display:grid;gap:12px}
.grid.cols-2{grid-template-columns:repeat(2,minmax(0,1fr))}
.grid.cols-3{grid-template-columns:repeat(3,minmax(0,1fr))}
@media (max-width:900px){ .grid.cols-3{grid-template-columns:repeat(2,minmax(0,1fr))} }
@media (max-width:620px){ .grid.cols-2,.grid.cols-3{grid-template-columns:1fr} }

.card{
  background:linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
  border:1px solid rgba(255,255,255,.10);
  border-radius:var(--r);
  padding:14px;
  box-shadow: var(--shadow);
}
.card h2,h3{margin:0 0 10px 0}
.sub{color:var(--muted);font-size:13px;line-height:1.35}

.btnrow{display:flex;gap:10px;flex-wrap:wrap;margin-top:10px}
.btn{
  border:0;cursor:pointer;
  padding:12px 14px;border-radius:14px;
  background:rgba(255,255,255,.06);
  color:var(--text);
  border:1px solid rgba(255,255,255,.12);
}
.btn.primary{
  background:linear-gradient(180deg, rgba(45,210,138,.95), rgba(26,143,90,.95));
  border:0;color:#062214;font-weight:800;
}
.btn.danger{
  background:rgba(255,77,77,.15);
  border:1px solid rgba(255,77,77,.35);
}
.btn:active{transform:translateY(1px)}

.field{display:grid;gap:8px;margin-top:10px}
.input{
  width:100%; padding:12px 12px; border-radius:14px;
  background:rgba(10,20,15,.65);
  border:1px solid rgba(255,255,255,.14);
  color:var(--text);
  outline:none;
}
.input::placeholder{color:rgba(184,214,198,.7)}

.kbd{
  padding:2px 8px;border-radius:999px;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.10);
  color:var(--muted);font-size:12px;
}

.list{display:grid;gap:10px;margin-top:12px}
.item{
  display:flex;align-items:center;justify-content:space-between;gap:10px;
  background:rgba(255,255,255,.05);
  border:1px solid rgba(255,255,255,.10);
  border-radius:16px;
  padding:12px 12px;
}
.item .left{display:flex;flex-direction:column;gap:3px}
.item .ttl{font-weight:900}
.item .meta{font-size:12px;color:var(--muted)}
.badge{
  padding:6px 10px;border-radius:999px;font-size:12px;
  border:1px solid rgba(255,255,255,.12);
  background:rgba(255,255,255,.06);
  color:var(--muted);
}

.wordgrid{display:grid;gap:12px;margin-top:12px}
.wordcard{
  display:grid;grid-template-columns:90px 1fr;gap:12px;
  padding:12px;border-radius:18px;
  background:linear-gradient(180deg, rgba(26,143,90,.14), rgba(255,255,255,.03));
  border:1px solid rgba(255,255,255,.12);
  box-shadow: var(--shadow);
}
.wordcard img{
  width:90px;height:90px;border-radius:18px;object-fit:cover;
  background:#0b1510;border:1px solid rgba(255,255,255,.10);
}
.wordcard .en{font-weight:1000;font-size:18px;letter-spacing:.2px}
.wordcard .ru{color:var(--muted);margin-top:2px}
.wordcard .actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
.small{
  padding:8px 10px;border-radius:12px;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.12);
  color:var(--text);
  cursor:pointer;
}

.toast{
  position:fixed; left:50%; bottom:18px; transform:translateX(-50%);
  background:rgba(0,0,0,.55);
  border:1px solid rgba(255,255,255,.14);
  padding:10px 12px;border-radius:14px;
  color:var(--text); display:none;
  max-width:92vw;
}
.toast.show{display:block;animation:pop .22s ease-out}
@keyframes pop{from{transform:translateX(-50%) translateY(8px);opacity:.4}to{transform:translateX(-50%) translateY(0);opacity:1}}
